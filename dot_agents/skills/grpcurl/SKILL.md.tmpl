---
name: grpcurl
description: Invoke a gRPC method against a remote endpoint. Auto-decides schema source (server reflection vs `buf build` descriptor), auth scheme, and payload shape — only asks the user when a choice is genuinely ambiguous.
metadata:
  short-description: Make ad-hoc gRPC calls against a service
---

{{ template "grpcurl-body.md" . }}
